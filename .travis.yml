language: python
dist: xenial
branches:
  only:
  - master
cache: pip
python:
  - "3.6"
  - "3.7"
env:
  global:
    - PYTEST_ADDOPTS=-vv
install:
  - pip install pipenv
  - pipenv run pip install pip==18.0
  - pipenv install --dev
script:
  - pipenv run lint
  - pipenv run test
